const moveUpRocket = () => {};

const toggleScene = () => {
  const scene = document.body.querySelector('.scene');
  scene.classList.toggle('hide');
};

const launchRocket = () => {
  toggleScene();
  // moveDownStar
  // flashScreen
};

export default launchRocket;
